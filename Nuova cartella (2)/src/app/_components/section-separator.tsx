/**
 * A presentational horizontal separator.
 *
 * Renders an `<hr>` element styled with Tailwind classes to provide vertical spacing
 * and a neutral border color: "border-neutral-200 mt-28 mb-24".
 *
 * @returns A JSX `<hr>` element used to visually separate content sections.
 */
export function SectionSeparator() {
  return <hr className="border-neutral-200 mt-28 mb-24" />;
}
