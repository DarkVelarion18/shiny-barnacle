/**
 * Renders a horizontal section separator.
 *
 * Returns an `<hr>` element styled with Tailwind classes `border-neutral-200`, `mt-28`, and `mb-24`.
 *
 * @returns A JSX `hr` element used as a visual section divider.
 */
export function SectionSeparator() {
  return <hr className="border-neutral-200 mt-28 mb-24" />;
}
